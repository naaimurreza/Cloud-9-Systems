while(!feof(file)) {
        //     fscanf(file,"Name: %s\nOrder ID: %d\nProducts(%d):\n", name, &id, &product_count);
        //     while(fgetc(file) != '^') {
        //         fscanf(file,"     1) Name: %s  -  Price: %f", product_name, &price);
        //         Product product;
        //         strcpy(product.name, product_name);
        //         product.price = price;
        //         products[j] = product;
        //     }
        //     fscanf(file,"Total: %f\n\n", &total);
        //     Order order;
        //     strcpy(order.username, name);
        //     order.order_id = id;
        //     order.product_count = product_count;
        //     order.total_price = total;
        //     orders[i++] = order;
        // }